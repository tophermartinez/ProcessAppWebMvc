@model IEnumerable<Entity_Layer.UNIDAD>

@{
    ViewBag.Title = "Read";
}

<h2>Read</h2>

<p>
    @Html.ActionLink("Create New", "Insert")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_UNIDAD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DETALLE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_UNIDAD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DETALLE)
        </td>
        <td>
            @Html.ActionLink("Editar", "Update", new { ID_UNIDAD = item.ID_UNIDAD }) |
            @Html.ActionLink("Delete", "Delete", new { ID_UNIDAD = item.ID_UNIDAD })
        </td>
    </tr>
}

</table>
